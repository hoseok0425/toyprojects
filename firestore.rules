rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null; // 인증된 사용자에게만 읽기 및 쓰기 권한 부여
    }
  }
}
